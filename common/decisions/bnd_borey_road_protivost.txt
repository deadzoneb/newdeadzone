bnd_borey_road_protivost = {

	dest_carteles_adm = {   

		#icon = tungsten

		allowed = {

		}

		highlight_states = {
			highlight_state_targets = {
				state = 40 
			}
		}

		available = { 
		check_variable={mdx > 50}
			has_manpower > 300
			has_equipment = { infantry_equipment > 299 }
			owns_state = 40 
			controls_state = 40 
date > 534.1.1
		}

		visible = {
OR = {
tag = ADM 
tag = RUP
}
has_idea = cmo_contrabandist_problem
			owns_state = 40 
			controls_state = 40  
		}

		fire_only_once = yes

		cost = 25
		days_remove = 60
 

		ai_will_do = {
			base = 10
		}
complete_effect = { 
add_manpower = -300
add_equipment_to_stockpile = { type = infantry_equipment amount = -300  }
		custom_effect_tooltip = potratit50
		subtract_from_variable = { var = mdx value = 50 }
}
		remove_effect = { 
add_political_power = 10
remove_ideas = cmo_contrabandist_problem
add_manpower = 200
add_equipment_to_stockpile = { type = infantry_equipment amount = 290  }
if = { limit = { BND = { has_country_flag = adm_oboidena } }
BND = { country_event = brrbnd.2  } }
		
if = { limit = { BND = { NOT = { has_country_flag = adm_oboidena } } }
BND = { country_event = brrbnd.3  } }
		custom_effect_tooltip = bnd_agressive
	}
}